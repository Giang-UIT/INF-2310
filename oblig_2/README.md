# How to run 


1) Run the server in a terminal (*python server.py*)   
2) Run the client in a seperate terminal (*python client.py*)

Once finished, a new file with the name "*recv_message.(the-chosen-format)*" will appear. 
That is the transfered file. 


# How to run the test program 
* Go into (*test.py*) and uncomment the test method for the desired test method. 
- To test the client. Run the server in a separate terminal and then runs the test program
- To test the server. Run the client in a separate terminal and then runs the test program